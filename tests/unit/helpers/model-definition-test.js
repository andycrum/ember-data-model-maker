import { modelDefinition } from '../../../helpers/model-definition';
import { module, test } from 'qunit';

module('Unit | Helper | model definition');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = modelDefinition(42);
  assert.ok(result);
});
